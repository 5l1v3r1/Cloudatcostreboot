# Cloud at cost reboot script

This script was written because the Cloud at Cost API has been down for months, so this will log in via panel.cloudatcost.com and reboot manually if your website is down
